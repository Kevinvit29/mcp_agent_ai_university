# Local-only overlay. Do not use this file on a public server.
services:
  mongo:
    ports:
      - "127.0.0.1:27018:27017"
  postgres:
    ports:
      - "127.0.0.1:5432:5432"
  mcp_server:
    ports:
      - "127.0.0.1:9000:9000"
  backend:
    ports:
      - "127.0.0.1:8000:8000"
